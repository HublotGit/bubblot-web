(function () {
    'use strict';
    angular
        .module('bubblot')
        .controller('mapCtrl', ['$timeout', '$scope', mapCtrl]);

    function mapCtrl($timeout,$scope) {
        var vm = this;
    }

}());
